VITE_MAX_UPLOAD_SIZE_BYTES=
# Set to a number (bytes) to enforce a client-side upload size limit.
# Example: 524288000  (500 MB)
# Leave empty for no client-side limit.
# The backend may enforce its own limit independently.
